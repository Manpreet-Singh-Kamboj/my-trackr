<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mytrackr.receipts">

    <!-- Permissions for camera and external storage -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTrackr">
        <!-- FileProvider for camera capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Receipt scanning activity -->
        <activity
            android:name=".features.receipts.ReceiptScanActivity"
            android:exported="false" />

        <activity
            android:name=".features.auth.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".features.auth.SignupActivity"
            android:exported="false"
            android:parentActivityName=".features.auth.SignInActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".features.auth.SignInActivity" />
        </activity>
        <activity
            android:name=".features.auth.SignInActivity"
            android:exported="false"
            android:parentActivityName=".features.get_started.GetStartedActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".features.get_started.GetStartedActivity" />
        </activity>
        <activity
            android:name=".features.get_started.GetStartedActivity"
            android:exported="false" />
        <activity
            android:name=".features.onboarding.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".features.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".features.core.MainActivity"
            android:exported="false" />
    </application>

</manifest>